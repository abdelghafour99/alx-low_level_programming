#include "main.h"
#include <stdio.h>

/**
 * main - program0
 *
 * @argc: number of agrument
 * @argv: array of arrguments
 * Return: Always 0.
 */
int main(int argc, char *argv[] __attribute__((unused)))
{
	printf("%d\n", argc - 1);
	return (0);
}
