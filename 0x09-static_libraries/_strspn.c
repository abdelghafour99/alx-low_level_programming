#include "main.h"
#include <stdio.h>

/**
 * _strspn - program15
 *
 * @s: the first string
 * @accept: the second string
 * Return: 0
 */
unsigned int _strspn(char *s, char *accept)
{
	int a;

	a = 0;

	return (a);
}
