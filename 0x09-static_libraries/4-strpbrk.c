#include "main.h"
#include <stdio.h>

/**
 * _strstr - program11
 *
 *@s: the first character
 *@accept: the second one
 * Return: Always 0
 */

char *_strpbrk(char *s, char *accept)
{

	return (0);
}
